import React, { useState } from "react";

const Profile = () => {
    return (
        <>
            <div>
                <button onClick={()=>{alert("hello")}}> Press </button>
            </div>
        </>
    );
};

export default Profile;